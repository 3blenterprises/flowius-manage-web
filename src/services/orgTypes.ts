export interface IFirebaseObject {
  id: string;
}

export interface Project {
  ProjectName: string;
  description: string;
  docId: string;
  Name: string;
  manageId: string;
  id: string;
  ProjectId: string;
}
